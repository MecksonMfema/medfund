-- Placeholder migration. The catalogue reshape + reason column that
-- were originally drafted here have moved to V041 so this file's
-- checksum stays stable — see feedback_never_edit_applied_migrations.
-- A no-op is safe (SELECT 1) and keeps the version slot recorded.
SELECT 1;
