{
  "name": "medfund-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E suite for the MedFund operational portal — stubs Keycloak and backend APIs so tests run deterministically without the full polyglot stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "~5.7.2"
  }
}
